#include <stdio.h>

int main()
{
    int num;

    printf("Enter any number to check even or odd: ");
    scanf("%d", &num);
    
    if(num % 2 == 0)
    {
        /* num % 2 is 0 */
        printf("Number is Even.");
    }
    else
    {
        /* num % 2 is 1 */
        printf("Number is Odd.");
    }

    return 0;
}
